apiVersion: v1
kind: Service
metadata:
  name: netflix-app
  namespace: netflix
  labels:
    app: netflix-app
spec:
  type: ClusterIP
  selector:
    app: netflix-app
  ports:
  - port: 80
    targetPort: 80
